import 'package:flutter/material.dart';

class VerificarCodigo extends StatelessWidget {
  const VerificarCodigo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
